#Generated by VisualGDB project wizard.
#Note: VisualGDB will automatically update this file when you add new sources to the project.

cmake_minimum_required(VERSION 2.7)
project(juk_main_control)
set(LIBRARIES_FROM_REFERENCES "")

find_package(roscpp REQUIRED)
include_directories(${roscpp_INCLUDE_DIRS})

find_package(GeoMath REQUIRED)
include_directories(${GeoMath_INCLUDE_DIRS})

find_package(juk_msg REQUIRED)
include_directories( ${juk_msg_INCLUDE_DIRS})

find_library(PTHREAD_LIBRARY pthread)

add_executable(juk_main_control juk_main_control.cpp)
target_link_libraries(juk_main_control /usr/local/lib/libGeoMath_lib.a ${PTHREAD_LIBRARY} ${roscpp_LIBRARIES} "${LIBRARIES_FROM_REFERENCES}")